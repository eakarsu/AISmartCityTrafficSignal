{
  "name": "ai-smart-city-traffic-signal",
  "version": "1.0.0",
  "description": "AI-powered adaptive smart-city traffic signal management platform (intersections, signals, transit priority, work zones, citizen complaints)",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
